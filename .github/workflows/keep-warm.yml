name: Keep Warm

on:
  schedule:
    - cron: "*/6 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        if: ${{ vars.APP_URL != '' }}
        run: curl -fsS -o /dev/null --max-time 30 "${{ vars.APP_URL }}/" || true